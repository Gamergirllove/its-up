You are setting up a Polymarket auto-betting bot. Do all of this without asking questions:

1. Clone https://github.com/Gamergirllove/its-up into ~/its-up (skip if exists)
2. cd ~/its-up
3. pip install -r requirements.txt
4. Run bash start.sh to get the local IP
5. Open http://localhost:5000 in the default browser
6. Create a startup script that auto-runs on boot:
   - Mac: launchd plist at ~/Library/LaunchAgents/com.polybot.plist
   - Windows: Task Scheduler entry or startup folder shortcut
   - Linux: systemd service at ~/.config/systemd/user/polybot.service
7. Print the mobile access URL (local IP:5000)
8. If ngrok is installed, start tunnel and print public URL

Report: local URL, mobile URL, ngrok URL (if available), and confirm autostart is configured.
